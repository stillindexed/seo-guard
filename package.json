{
  "name": "seo-guard",
  "version": "1.0.1",
  "description": "GitHub Action: fail a deploy that introduces noindex, robots blocks, canonical regressions, or unexpected status codes.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node scripts/build.mjs"
  },
  "private": true
}
